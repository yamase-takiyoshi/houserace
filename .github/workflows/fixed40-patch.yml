name: Apply FIXED40 NAR official mobile odds

on:
  push:
    paths:
      - '.github/workflows/fixed40-patch.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Patch index.html
        run: python tools/fixed40_patch.py

      - name: Commit FIXED40 and remove one-shot files
        run: |
          rm -f .github/workflows/fixed40-patch.yml tools/fixed40_patch.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html .github/workflows/fixed40-patch.yml tools/fixed40_patch.py
          git commit -m "FIXED40: NAR official mobile odds first"
          git push origin HEAD:main
